# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InstaFam' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InstaFam
  pod 'Parse'
  pod 'Toucan' #resizing images
  pod 'ParseUI' #displaying images via PFImageView
  target 'InstaFamTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InstaFamUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
